version: 1
services:
  - type: web
    name: agri-guard-backend
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    autoDeploy: true
    envVars:
      - key: FLASK_ENV
        value: development
      - key: PORT
        value: 5000
